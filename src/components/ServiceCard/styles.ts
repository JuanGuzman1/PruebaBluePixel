import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  image: {
    aspectRatio: '16/9',
    borderRadius: 10,
  },
  title: {
    fontSize: 18,
    fontWeight: '500',
    letterSpacing: 0.3,
  },
});

export default styles;
